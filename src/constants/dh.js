const bigInt = require("big-integer");

const prime = bigInt('1234567890'); // Substitua pelo seu número primo desejado
const primitiveRoot = bigInt('3'); // Substitua pela sua raiz primitiva desejada

module.exports = { prime, primitiveRoot }